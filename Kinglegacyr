local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

-- --- [ CẤU HÌNH VÙNG QUÉT ] ---
local mainCFrame = CFrame.new(20072.912109375, 15584.1640625, 20049.546875)
local scanSize = Vector3.new(1500, 10000, 1500) -- Chỉ quét trong phạm vi này
local closeSize = Vector3.new(10, 10, 10)

local skillRemote = ReplicatedStorage:WaitForChild("Chest"):WaitForChild("Remotes"):WaitForChild("Functions"):WaitForChild("SkillAction")

local autoRunning = false
local currentTargetCFrame = nil -- Vị trí để ghim nhân vật lơ lửng

-- --- [ GIAO DIỆN ] ---
local screenGui = player:WaitForChild("PlayerGui"):FindFirstChild("AutoSkillGui") or Instance.new("ScreenGui")
screenGui.Name = "AutoSkillGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = player:WaitForChild("PlayerGui")

local toggleButton = screenGui:FindFirstChild("ToggleButton") or Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0, 150, 0, 50)
toggleButton.Position = UDim2.new(0, 20, 0, 100)
toggleButton.Text = "Bật Auto Skill"
toggleButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
toggleButton.TextColor3 = Color3.new(1, 1, 1)
toggleButton.TextScaled = true
toggleButton.Parent = screenGui

-- --- [ HÀM HỖ TRỢ ] ---

-- Quét NPC giới hạn trong Box và loại bỏ Player
local function getNPCs()
	local npcs = {}
	local foundModels = {}
	-- Sử dụng hàm đời mới để quét vùng chính xác và nhẹ máy
	local partsInRegion = Workspace:GetPartBoundsInBox(mainCFrame, scanSize)
	
	for _, part in ipairs(partsInRegion) do
		local model = part:FindFirstAncestorOfClass("Model")
		if model and not foundModels[model] and model:FindFirstChild("Humanoid") and model:FindFirstChild("HumanoidRootPart") then
			-- Fix lỗi: Kiểm tra không phải bản thân và không phải người chơi khác
			if model ~= character and not Players:GetPlayerFromCharacter(model) then
				foundModels[model] = true
				table.insert(npcs, model)
			end
		end
	end
	return npcs
end

local function getAveragePosition(npcs)
	local sum = Vector3.new(0, 0, 0)
	local count = 0
	for _, npc in ipairs(npcs) do
		local hrp = npc:FindFirstChild("HumanoidRootPart")
		if hrp then
			sum += hrp.Position
			count += 1
		end
	end
	return count > 0 and (sum / count) or mainCFrame.Position
end

local function areNPCsClose(npcs)
	for i = 1, #npcs do
		for j = i + 1, #npcs do
			local hrp1 = npcs[i]:FindFirstChild("HumanoidRootPart")
			local hrp2 = npcs[j]:FindFirstChild("HumanoidRootPart")
			if hrp1 and hrp2 then
				local diff = (hrp1.Position - hrp2.Position)
				if math.abs(diff.X) > closeSize.X or math.abs(diff.Y) > closeSize.Y or math.abs(diff.Z) > closeSize.Z then
					return false
				end
			end
		end
	end
	return true
end

local function useSkillAt(targetCFrame)
	local args = {
		"DF_GoldGold_V",
		{
			MouseHit = targetCFrame,
			Mobile = true
		}
	}
	skillRemote:InvokeServer(unpack(args))
end

-- --- [ VÒNG LẶP CHÍNH ] ---

-- Cơ chế lơ lửng: Teleport liên tục mỗi frame (Thay thế Gravity)
RunService.Heartbeat:Connect(function()
	if autoRunning and currentTargetCFrame and humanoidRootPart then
		humanoidRootPart.CFrame = currentTargetCFrame
		humanoidRootPart.Velocity = Vector3.new(0, 0, 0) -- Triệt tiêu lực rơi
	end
end)

-- Kill Aura an toàn (Chỉ tác động NPC trong vùng quét)
local function killAuraLoop()
	getgenv().G = true
	getgenv().Creator = 'https://discord.gg/B3HqPPzFYr - HalloweenGaster'
	
	while getgenv().G and autoRunning do
		task.wait(0.5)
		pcall(function()
			sethiddenproperty(player, "SimulationRadius", 200)
			sethiddenproperty(player, "MaxSimulationRadius", 200)
		end)
		
		local targetNPCs = getNPCs()
		for _, npc in ipairs(targetNPCs) do
			local hum = npc:FindFirstChild("Humanoid")
			if hum then hum.Health = 0 end
		end
	end
end

-- Logic điều khiển Teleport và Skill
local function autoSkillLoop()
	while autoRunning do
		character = player.Character or player.CharacterAdded:Wait()
		humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
		
		local npcs = getNPCs()
		if #npcs > 0 then
			local targetNPC = nil
			
			-- Ưu tiên Boss
			for _, npc in ipairs(npcs) do
				if npc.Name == "Heartbreaker Queen [Lv. 5000]" then
					targetNPC = npc
					break
				end
			end
			
			if targetNPC then
				local pos = targetNPC.HumanoidRootPart.CFrame
				currentTargetCFrame = pos * CFrame.new(0, -15, 0) -- Đứng dưới 15 stud
				useSkillAt(pos)
				task.wait(1)
			elseif #npcs == 1 then
				local pos = npcs[1].HumanoidRootPart.CFrame
				currentTargetCFrame = pos * CFrame.new(0, -15, 0)
				useSkillAt(pos)
			else
				-- Gom quái hoặc đánh vào trung tâm
				local avgPos = getAveragePosition(npcs)
				local targetCF = CFrame.new(avgPos)
				currentTargetCFrame = targetCF * CFrame.new(0, -15, 0)
				useSkillAt(targetCF)
			end
		else
			-- Nếu không có quái, quay về tâm vùng quét lơ lửng chờ
			currentTargetCFrame = mainCFrame * CFrame.new(0, 50, 0)
		end
		task.wait(0.5)
	end
end

-- --- [ ĐIỀU KHIỂN ] ---
toggleButton.MouseButton1Click:Connect(function()
	autoRunning = not autoRunning
	toggleButton.Text = autoRunning and "Tắt Auto Skill" or "Bật Auto Skill"
	toggleButton.BackgroundColor3 = autoRunning and Color3.fromRGB(170, 0, 0) or Color3.fromRGB(0, 170, 0)
	
	if autoRunning then
		task.spawn(autoSkillLoop)
		task.spawn(killAuraLoop)
	else
		getgenv().G = false
		currentTargetCFrame = nil
	end
end)
